= CppTest Utility
:toc: macro
:icons: font

**CppTest** is a powerful command-line tool for competitive programming. It automates compilation, static analysis, test generation, and stress testing.

toc::[]

== ✨ Features

*   **🧪 Test Generation:** Automatically compiles `gen.cpp` and generates inputs.
*   **🔥 Stress Testing:** Flag `-S` runs a brute-force comparison (`main` vs `fast`) on generated tests.
*   **🛡️ Isolation:** By default, tests run in `/tmp`, keeping your project clean.
*   **✨ Auto-Fix:** Flag `-F` fixes formatting and linter errors.
*   **🧹 Clean Compile:** Flag `-R` removes comments before compilation.
*   **🔍 Static Analysis:** Clang-tidy & Clang-format integration.
*   **🤖 Custom Checkers:** Supports `check.cpp`.

== 📦 Installation

[source,bash]
----
rm -rf /tmp/cpptest_inst && git clone --depth=1 https://github.com/MootSasa/CppTest.git /tmp/cpptest_inst && bash /tmp/cpptest_inst/install.sh && rm -rf /tmp/cpptest_inst
----

== 🛠 Usage

=== Basic Run
[source,bash]
----
cpptest             # Run in current dir
cpptest -S          # Run stress testing
cpptest -u          # Update utility
----

=== Generator Support (`gen.cpp`)
If `gen.cpp` exists, CppTest compiles it and generates tests before running `main.cpp`.
Generated files are named `gen_1.in`, `gen_2.in`...

**Configuration (`tests.cfg`):**
Create a file named `tests.cfg` to define test groups.
Format: `Count Arguments`
```text
10 1 100   # Run 10 times: ./gen 1 100
5 500 1000 # Run 5 times: ./gen 500 1000
